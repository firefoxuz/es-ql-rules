name: Mass Query on Payment Database
description: |
  Identifies bulk SELECT queries on tables containing cardholder data.
type: esql
params: '{}'
schedule_interval: 15m
index:
  - filebeat-*
  - auditbeat-*
query_language: esql
query: |
  FROM filebeat-*, auditbeat-*
  | WHERE @timestamp >= NOW() - 15 minutes
  | WHERE message RLIKE ".*SELECT.*FROM.*(payment|transaction|card|cardholder).*"
    OR process.args RLIKE ".*SELECT \\* FROM.*"
  | WHERE message NOT RLIKE ".*(LIMIT [0-9]{1,2}|WHERE id =).*"
  | STATS query_count = COUNT(*) BY user.name, host.name
  | WHERE query_count >= 5
  | EVAL severity = "high"
  | KEEP @timestamp, user.name, host.name, query_count, message, severity
enabled: false
tags:
  - system-integrity
severity: high
risk_score: 75
# MITRE ATT&CK: tactic external_id -> technique external_id -> [subtechnique external_ids]
mitre_attack: {}
nist: []
gdpr: []
pci-dss: ["3.3.1"]
hipaa: []
