rule_id: 78b8d9af-3e89-4475-b615-8fe69212e2e8
name: Computer Account Deleted From Active Directory
description: |
  Ushbu qoida credential access va privilege escalation texnikalariga bog'liq bo'lib, `Computer Account Deleted From Active Directory` kontekstidagi shubhali Active Directory faoliyatini aniqlaydi.
  Bu turdagi hodisa AD muhitida hujumchiga ruxsatsiz kirishni mustahkamlash, hisoblarni egallash yoki boshqaruv huquqlarini oshirish imkonini berishi mumkinligi sababli xavfli hisoblanadi.
  Qonuniy holatlar sifatida rejalashtirilgan administrativ ishlar, change/deployment jarayonlari, IAM sinxronizatsiyasi yoki servis konfiguratsiya yangilanishlari uchrashi mumkin.
  SOC triage bosqichlari:
  - Hodisadagi `actor` va `target_user` (yoki target obyekt) ni aniq identifikatsiya qiling.
  - O'zgarish/kirish uchun tasdiqlangan change ticket yoki deployment yozuvi borligini tekshiring.
  - `src_ip` manzili, manba host va tarmoq segmentining legitimligini tekshiring.
  - Ta'sirlangan akkaunt yoki obyektning guruh a'zoligi, ayniqsa imtiyozli guruhlar bilan bog'liqligini tekshiring.
  - Noqonuniy faoliyat tasdiqlansa tezkor containment bajaring: manbani cheklang, credentiallarni yangilang va ta'sir doirasini izolyatsiya qiling.
type: esql
params: '{}'
schedule_interval: 10m
index:
- logs-winlog*
query_language: esql
query: |
  FROM logs-winlog*
  | WHERE @timestamp >= NOW() - 15 minutes
  | WHERE winlog.channel == "Security"
  | EVAL hostn = winlog.computer_name
  | EVAL actor = COALESCE(winlog.event_data.SubjectUserName, winlog.event_data.SubjectUserName)
  | EVAL target_user = COALESCE(winlog.event_data.TargetUserName, winlog.event_data.SamAccountName)
  | EVAL src_ip = COALESCE(winlog.event_data.IpAddress, winlog.event_data.IpAddress)
  | WHERE hostn RLIKE "(?i)(WINDC|ADDC|-DC|DOMAINCONTROLLER)"
  | WHERE NOT actor RLIKE ".*\\$$"
  | WHERE event.code == "4743"
  | EVAL 
      deleter = COALESCE(winlog.event_data.SubjectUserName, winlog.event_data.SubjectUserName, "Unknown"),
      deleted_computer = COALESCE(winlog.event_data.TargetUserName, "Unknown"),
      computer_sid = COALESCE(winlog.event_data.TargetSid, "Unknown")
  | STATS 
      computers_deleted = COUNT(*),
      deleted_computers = VALUES(deleted_computer),
      computer_sids = VALUES(computer_sid),
      first_deletion = MIN(@timestamp),
      last_deletion = MAX(@timestamp)
    BY winlog.computer_name, deleter, bucket = DATE_TRUNC(1 hour, @timestamp)
  | WHERE computers_deleted >= 1
  | EVAL 
      severity_level = CASE(
        computers_deleted >= 10, "Critical - Mass Deletion",
        computers_deleted >= 5, "High - Multiple Deletions",
        "Medium - Single Deletion"
      )
  | KEEP agent_id, bucket, winlog.computer_name, deleter, computers_deleted, deleted_computers,
      computer_sids, severity_level, first_deletion, last_deletion
  | SORT computers_deleted DESC
  | LIMIT 200
enabled: false
tags:
  - attack.impact
  - attack.defense-evasion
  - attack.t1485
  - detection.configuration-change
severity: high
risk_score: 75
mitre_attack:
  TA0040:
    T1485: []
  TA0005:
    T1070: []
nist: ["AU-6", "AU-9", "SI-4"]
pci_dss: ["10.2.7", "10.5"]
gdpr: ["32.1.a", "32.2"]
