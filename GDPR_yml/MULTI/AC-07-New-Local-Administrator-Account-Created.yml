name: New Local Administrator Account Created
description: |
  Detects creation of new local administrator accounts on Windows or Unix systems.
type: esql
params: '{}'
schedule_interval: 10m
index:
  - winlogbeat-*
  - filebeat-*
  - auditbeat-*
query_language: esql
query: |
  FROM winlogbeat-*, filebeat-*, auditbeat-*
  | WHERE @timestamp >= NOW() - 10 minutes
  | WHERE (event.code == "4720" AND winlog.event_data.MemberSid RLIKE ".*-500") 
      OR (event.action == "added-user-account" AND user.name RLIKE ".*(admin|root).*")
      OR (process.name == "useradd" AND process.args RLIKE ".*-G (sudo|wheel|admin).*")
  | EVAL severity = "high"
  | KEEP @timestamp, host.name, user.name, event.action, process.name, process.args, severity
enabled: false
tags:
  - access-control
severity: high
risk_score: 75
# MITRE ATT&CK: tactic external_id -> technique external_id -> [subtechnique external_ids]
mitre_attack: {}
nist: []
gdpr: ["II.5.1.f", "IV.32.1"]
pci-dss: []
hipaa: []
