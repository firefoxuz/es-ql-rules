name: First-Time Login from Geographic Location
description: |
  Identifies first-ever successful login from a new country/region for a user account.
type: esql
params: '{}'
schedule_interval: 15m
index:
  - winlogbeat-*
  - filebeat-*
query_language: esql
query: |
  FROM winlogbeat-*, filebeat-*
  | WHERE @timestamp >= NOW() - 15 minutes
  | WHERE event.category == "authentication" AND event.outcome == "success"
  | WHERE source.geo.country_name IS NOT NULL
  | STATS countries = VALUES(source.geo.country_name) BY user.name
  | WHERE LENGTH(countries) >= 2
  | EVAL severity = "medium"
  | KEEP @timestamp, user.name, source.ip, source.geo.country_name, countries, severity
enabled: false
tags:
  - access-control
severity: medium
risk_score: 50
# MITRE ATT&CK: tactic external_id -> technique external_id -> [subtechnique external_ids]
mitre_attack: {}
nist: []
gdpr: ["IV.32.1", "V.44"]
pci-dss: []
hipaa: []
