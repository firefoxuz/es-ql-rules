name: Login Outside Business Hours (High-Privilege Account)
description: |
  Alerts on privileged account logins during non-business hours (22:00-06:00 weekdays, all weekend).
type: esql
params: '{}'
schedule_interval: 15m
index:
  - winlogbeat-*
  - filebeat-*
query_language: esql
query: |
  FROM winlogbeat-*, filebeat-*
  | WHERE @timestamp >= NOW() - 15 minutes
  | WHERE event.category == "authentication" AND event.outcome == "success"
  | WHERE user.name RLIKE ".*(admin|root|sysadmin).*"
  | EVAL hour = DATE_EXTRACT(@timestamp, "hour")
  | EVAL day_of_week = DATE_EXTRACT(@timestamp, "day_of_week")
  | WHERE (hour < 6 OR hour >= 22) OR (day_of_week IN (6, 7))
  | EVAL severity = "medium"
  | KEEP @timestamp, user.name, source.ip, host.name, hour, day_of_week, severity
enabled: false
tags:
  - access-control
severity: medium
risk_score: 50
# MITRE ATT&CK: tactic external_id -> technique external_id -> [subtechnique external_ids]
mitre_attack: {}
nist: []
gdpr: ["II.5.1.f", "IV.32.1"]
pci-dss: []
hipaa: []
