name: Lateral Movement Pattern Detected
description: |
  Detects authentication from one host to multiple other hosts within 10 minutes (lateral movement).
type: esql
params: '{}'
schedule_interval: 10m
index:
  - winlogbeat-*
  - filebeat-*
query_language: esql
query: |
  FROM winlogbeat-*, filebeat-*
  | WHERE @timestamp >= NOW() - 10 minutes
  | WHERE event.category == "authentication" AND event.outcome == "success"
    AND event.action IN ("logon", "network_logon")
  | STATS unique_destinations = COUNT_DISTINCT(destination.ip) BY user.name, source.ip
  | WHERE unique_destinations >= 5
  | EVAL severity = "high"
  | KEEP @timestamp, user.name, source.ip, unique_destinations, severity
enabled: false
tags:
  - incident-response
severity: high
risk_score: 75
# MITRE ATT&CK: tactic external_id -> technique external_id -> [subtechnique external_ids]
mitre_attack: {}
nist: []
gdpr: ["IV.32.1", "IV.33"]
pci-dss: []
hipaa: []
