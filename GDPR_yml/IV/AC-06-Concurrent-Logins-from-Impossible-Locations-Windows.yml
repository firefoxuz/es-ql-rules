name: Concurrent Logins from Different IPs - Windows
description: |
  Detects successful Windows logons (Event ID 4624) from multiple source IPs within 1 hour for the same user.
type: esql
params: '{}'
schedule_interval: 1h
index:
  - winlogbeat-*
  - filebeat-*
query_language: esql
query: |
  FROM winlogbeat-*, filebeat-*
  | WHERE @timestamp >= NOW() - 1 hour
  | WHERE event.category == "authentication" AND event.outcome == "success"
  | WHERE source.geo.location IS NOT NULL
  | STATS 
      locations = VALUES(source.geo.location),
      countries = VALUES(source.geo.country_name),
      time_span = MAX(@timestamp) - MIN(@timestamp)
    BY user.name
  | WHERE LENGTH(countries) >= 2 AND time_span < 3600000
  | EVAL severity = "high"
  | KEEP @timestamp, user.name, countries, locations, time_span, severity
enabled: false
tags:
  - access-control
severity: high
risk_score: 75
# MITRE ATT&CK: tactic external_id -> technique external_id -> [subtechnique external_ids]
mitre_attack: {}
nist: []
gdpr: ["IV.32.1.b", "IV.33"]
pci-dss: ["12.10.1"]
hipaa: []
