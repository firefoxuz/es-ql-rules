rule_id: c3cfa8ed-048c-4db6-9881-f2b6383f3f61
name: VMware ESX - Multiple User Auth Failures via hostd/authd from Same Source (Brute Force)
description: |
  120 soniya ichida bir xil manba IP'dan vmware-hostd yoki vmware-authd orqali
  8 va undan ortiq parol rad etish holati qayd etildi (T1110, T1497).
  Bu maqsadli brute force hujumining aniq belgisi.
  SOC triage bosqichlari:
  - vmware.originator orqali manba IP'ni aniqlang va bloklang
  - Hujum qilinayotgan foydalanuvchi hisoblarini tekshiring
  - vmware-hostd vs vmware-authd qaysi daemon hujumga uchragan aniqlang
  - vmware.sid orqali sessiyani kuzating
  - Muvaffaqiyatli login urinishlari bilan korrelyatsiya qiling
type: esql
params: '{}'
schedule_interval: 5m
index:
  - logs-vmware.esxi*
query_language: esql
query: |
  FROM logs-vmware.esxi*
  | WHERE @timestamp >= NOW() - 10 minutes
  | WHERE event.module == "vmware"
  | WHERE event.dataset == "vmware.esxi"
  | WHERE event.outcome == "failure"
  | WHERE event.category == "authentication"
  | WHERE process.name IN ("vmware-hostd", "vmware-authd")
  | WHERE message LIKE "*Rejected password for*"
  | STATS
      reject_count        = COUNT(*),
      affected_users      = VALUES(user.name),
      unique_users        = COUNT_DISTINCT(user.name),
      affected_hosts      = VALUES(host.hostname),
      unique_hosts        = COUNT_DISTINCT(host.hostname),
      process_names       = VALUES(process.name),
      process_ids         = VALUES(process.pid),
      thread_ids          = VALUES(process.thread.id),
      originator_list     = VALUES(vmware.originator),
      opid_list           = VALUES(vmware.opid),
      sid_list            = VALUES(vmware.sid),
      syslog_priority     = VALUES(log.syslog.priority),
      syslog_version      = VALUES(log.syslog.version),
      log_logger          = VALUES(log.logger),
      log_messages        = VALUES(message),
      first_seen          = MIN(@timestamp),
      last_seen           = MAX(@timestamp)
    BY host.hostname, process.name, vmware.originator, bucket = DATE_TRUNC(2 minutes, @timestamp)
  | WHERE reject_count >= 8
  | KEEP
      bucket, host.hostname, process.name, vmware.originator, reject_count,
      affected_users, unique_users,
      affected_hosts, unique_hosts,
      process_ids, thread_ids,
      opid_list, sid_list,
      syslog_priority, syslog_version, log_logger,
      log_messages, first_seen, last_seen
  | SORT reject_count DESC
  | LIMIT 50
enabled: true
tags:
  - attack.credential-access
  - attack.t1110
  - attack.defense-evasion
  - attack.t1497
  - vmware
  - esxi
  - authentication
  - brute-force
  - hostd
  - authd
  - same-source
severity: high
risk_score: 73
references:
  - "https://attack.mitre.org/techniques/T1110/"
  - "https://attack.mitre.org/techniques/T1497/"
nist: ["AC-7", "AU-14", "SI-4"]
pci_dss: ["10.2.4", "10.2.5", "11.4"]
gdpr: ["35.7.d", "32.2"]
hipaa: ["164.312.b"]
mitre_attack:
  TA0006:
    T1110: []
  TA0005:
    T1497: []